import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  header: {
    textAlign: "center",
    fontSize: 18,
    fontWeight: "bold",
    borderBottomWidth: 2,
  },
});

export default styles;
